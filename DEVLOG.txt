WRANGLE DEVLOG
==============
One entry per update iteration. Newest entry at the TOP.
Claude: read this file at the start of every session to see where work left
off, and append a new entry every time an iteration of work is completed.

----------------------------------------------------------------------
[M1.6] 2026-07-06 - Sprites keep original filenames; source folder gone
----------------------------------------------------------------------
- Per user request: public/sprites/ now holds files under their ORIGINAL
  names (e.g. "Aquarix (Male)_100x100.png", spaces/parens and all) -
  no more auto-renaming to <id>.png. The one-off "creature sprites/"
  staging folder is deleted; from now on the user just drops new PNGs
  straight into public/sprites/ under whatever name they already use.
- Since filenames no longer match ids, each creatures.csv row now has
  an explicit textureKey = exact filename (no .png). species.ts's
  blank-textureKey-falls-back-to-id default still applies for any
  FUTURE creature whose sprite happens to be saved as <id>.png.
- BootScene.preload now encodeURIComponent()s the sprite path (spaces/
  parens are invalid raw in a URL) while keeping the Phaser texture key
  as the literal filename.
- docs/CREATURES.md sprite gallery links updated to the real filenames
  (percent-encoded for markdown/GitHub rendering).
- STATUS: awaiting playtest confirming sprites still load correctly
  with the space/paren filenames.

----------------------------------------------------------------------
[M1.5] 2026-07-06 - Real sprites + striped rope + western HUD
----------------------------------------------------------------------
- User uploaded 48 pixel-art sprites into "creature sprites/" (source
  folder, keep) + re-uploaded old-schema CSV. Copied/renamed all
  sprites to public/sprites/<id>.png (aquarix male=default + aquarix-f,
  mega-bloomancer + mega-ramageddon staged unused; skipped stray
  "Designer-pixel-snapper" file).
- CSV restructured again to the M1.4 schema (type1/type2 + hp/attack/
  defense/spAttack/spDefense/speed + attackPattern). Ramageddon name
  finalized (no /Fleecifer). textureKey column now BLANK by default:
  it falls back to the id, BootScene preloads public/sprites/<id>.png,
  and scenes fall back to pl-unknown if the file is missing (loaderror
  logged to console).
- Striped lasso: dark outline pass + alternating light/dark bands every
  14px of arclength (drawRopeStripes), knot upgraded. Rope palette
  constants at top of CaptureScene.
- Western pixel HUD: leather panels with stitched edges top+bottom,
  HEALTH segments are wood-framed sockets with corner rivets; fill
  stays RED with pixel bevel (highlight top / shade bottom). Capture
  gauge frame matched to the same palette.
- REMINDER told to user: upload the public/ folder (new) to GitHub.
- STATUS: awaiting playtest with real art. Next: capture stats/behavior
  tuning per creature in CSV, or M2/M3.

----------------------------------------------------------------------
[M1.4] 2026-07-06 - User's 45-creature dex + stats columns + scroll UI
----------------------------------------------------------------------
- User added their 45 original creatures to creatures.csv on GitHub
  (fetched from the public repo - remember: sync GitHub-side edits into
  this folder before editing locally!). Test creatures (bessie/pecksy/
  dustdevil) removed; CSV row order = dex order.
- CSV schema expanded: type -> type1 + type2 columns; new battle stat
  columns hp/attack/defense/spAttack/spDefense/speed (stored in
  SpeciesDef, used from M3); capture-attack column renamed
  attackPattern (avoids clash with the attack stat).
- Parser now applies defaults for blank capture columns (graze, loops
  3, radius 40, speed 100, no attacks, pl-unknown mystery-blob sprite,
  id auto-derived from name) + duplicate-id validation, so name+type
  rows are immediately playable.
- CaptureSelectScene rebuilt as a scrollable CRITTER DEX grid (3 cols,
  drag to scroll, tap-vs-drag slop 12px, dex numbers #001+, type
  labels). Old 3-card layout couldn't hold 45.
- BootScene: new pl-unknown placeholder texture.
- STATUS: user filling in creature stats/behaviors in Excel over time.
  Next likely: real sprite PNG loading from public/sprites/, or M2/M3.

----------------------------------------------------------------------
[M1.3] 2026-07-06 - Whole-bar damage + creatures.csv spreadsheet
----------------------------------------------------------------------
Playtest feedback round 3 (decay pacing approved):
- HEALTH is now counted in whole bars (5). Attacks remove whole bars
  only - no partial segment fills. attackDamage in creature data now
  means "bars per hit" (dustdevil: 1).
- NEW: creature data moved from TS literals to src/data/creatures.csv,
  a real spreadsheet the user edits in Excel/Numbers/Sheets. Vite
  imports it with ?raw; species.ts now parses + validates it at build
  time (descriptive per-row errors name the creature + column). Added
  src/vite-env.d.ts for the ?raw import type. New columns: type,
  atkStyle (stored now, used by M3 battles).
- docs/CREATURES.md reworked into the how-to-edit-the-CSV guide with
  full column reference + sprite gallery section.
- NOTE: species.ts had a UTF-8 mangling incident via PowerShell
  Get-Content/Set-Content (em-dashes corrupted); rewritten clean with
  plain hyphens. Lesson: never round-trip project files through
  PS 5.1 default encoding - use the Write tool.
- STATUS: awaiting playtest round 4. User can now try adding a test
  creature row to creatures.csv themselves.

----------------------------------------------------------------------
[M1.2] 2026-07-06 - Gauge decay replaces stun; HEALTH; no rope budget
----------------------------------------------------------------------
Playtest feedback round 2 ("new ranger line works great!"):
- REMOVED the dazed/stun mechanic entirely (release-mid-draw now just
  drops the line, no penalty; CreatureActor stun state + riled speedup
  deleted).
- NEW anti-stall pressure: if 4s pass without closing a loop around the
  creature, the capture gauge drains quickly (GAUGE_DECAY_DELAY_S=4,
  GAUGE_DECAY_PER_S=0.75 loops/s; gaugeProgress is now a float).
- RENAMED GRIT -> HEALTH everywhere (UI label, toasts, docs).
- Lasso line thicker: LINE_WIDTH 5 -> 7.
- REMOVED the rope length meter + budget mechanic (LassoLine now gets
  Infinity budget; may return later as an upgrade axis). Bottom strip
  now shows only the segmented HEALTH bar; arena grew taller.
- STATUS: awaiting playtest round 3 (decay pacing: is 4s + 0.75/s fair
  on PECKSY/DUSTDEVIL?).

----------------------------------------------------------------------
[M1.1] 2026-07-06 - iPhone fit + Ranger-accurate line + UI rework
----------------------------------------------------------------------
Playtest feedback round 1 (tested on Mac browser + iPhone Safari):
- FIXED iPhone sizing: black bar top / bottom cut off. index.html now
  uses 100dvh (Safari's collapsing chrome broke 100vh) + viewport-fit=
  cover; main.ts computes logical height from device aspect (720 wide,
  1280-1600 tall) so tall phones fill the screen. Scenes now derive
  layout from this.scale.height instead of hardcoded 1280.
- FIXED loop rule to match Pokemon Ranger Shadows of Almia: closing a
  loop consumes ONLY the loop portion; the pre-loop tail stays on the
  field (loopable, breakable), drawing continues from the crossing
  point, loop length is refunded to the rope budget (LassoLine.ts).
- UI REWORK per user: removed the loop pips (top-left orange circles -
  user found them confusing); capture progress is now a gauge that
  follows below the creature, +10 floats up per loop, full gauge =
  captured. GRIT bar is now SEGMENTED (5 segments of 20) and moved to
  the bottom strip next to the rope meter.
- STATUS: awaiting playtest round 2 on iPhone (fit + loop feel).

----------------------------------------------------------------------
[M1] 2026-07-05 - Lasso capture prototype
----------------------------------------------------------------------
- Built the full Pokemon Ranger-style capture mini-game (CaptureScene):
  loop drawing, rope budget (resets per loop), banked-loop pips,
  body-touch line break (no damage), telegraphed radial attacks that
  break the line AND damage GRIT (lasso HP), release-mid-draw stun with
  anti-cheese (no progress during stun, creature gets faster after).
- Pure-math capture modules: geometry.ts (segment intersection,
  point-in-polygon, point-to-segment), LassoLine.ts, CreatureActor.ts
  (graze / flee / charge movement AI + attack telegraph state machine).
- 3 placeholder targets in src/data/species.ts: BESSIE cow (graze, 2
  loops), PECKSY hen (flee, 3 loops), DUSTDEVIL (charge + radial burst,
  4 loops, 25 dmg). Sprites are runtime-generated placeholder graphics.
- Scene flow: Boot (texture gen + Silkscreen font load) -> Title ->
  CaptureSelect -> Capture. Shared button helper in src/ui/button.ts.
- Docs: docs/GAME_DESIGN.md (full concept), docs/ROADMAP.md (M0-M10).
- Renamed README.md -> GUIDE.md (user keeps their own README on GitHub).
- STATUS: awaiting first playtest feedback (loop feel, rope budget 1600,
  Dustdevil fairness). Tuning knobs: top of CaptureScene.ts + species.ts.

----------------------------------------------------------------------
[M0] 2026-07-04 - Project scaffold
----------------------------------------------------------------------
- Phaser 3 + TypeScript + Vite scaffold; portrait 720x1280, pixelArt,
  Silkscreen font via Google Fonts in index.html.
- Pipeline verified end-to-end: Windows (Claude writes files) -> user
  manually uploads to their GitHub repo -> Mac (git pull, npm run dev,
  browser playtest).
- Mac environment verified: Node 26.4.0, npm 11.17.0, CocoaPods 1.16.2,
  Xcode + iOS 26.5 simulator. Apple Developer Program NOT purchased yet
  (not needed until TestFlight/App Store).
